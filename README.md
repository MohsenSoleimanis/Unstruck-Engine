# MAS — Multi-Agent System

Production-scale, data-agnostic multi-agent orchestration built with LangGraph.

## Quick Start

```bash
# Backend
pip install -e .
mas serve --port 8000

# Frontend
cd ui && npm install && npm run dev
```

Open http://localhost:5173
